[workspace]
members = [
    "event-system",
    "event-system-derive",
    "scheduler-bindings",
    "scheduler-handshake",
    "transaction-view",
]

resolver = "2"

[workspace.package]
authors = ["Anza Maintainers <maintainers@anza.xyz>"]
repository = "https://github.com/anza-xyz/agave-sdk"
homepage = "https://anza.xyz"
license = "Apache-2.0"
edition = "2024"
rust-version = "1.89.0"

# Clippy lint configuration that can not be applied in clippy.toml
[workspace.lints.clippy]
# Denied lints
arithmetic_side_effects = "deny"
default_trait_access = "deny"
manual_let_else = "deny"
uninlined_format_args = "deny"
used_underscore_binding = "deny"

# Allowed lints
new_without_default = "allow"

[workspace.dependencies]
# workspace members
agave-event-system-derive = { path = "event-system-derive" }
agave-scheduler-bindings = { path = "scheduler-bindings", version = "6.0.0" }

bytes = "1.12.1"
criterion = "0.8.2"
libc = "0.2.189"
nix = "0.31.3"
proc-macro-crate = "3.5.0"
proc-macro2 = "1.0.106"
quote = "1.0.46"
rstest   = "0.26.1"
rts-alloc = "5.1.0"
shaq = "4.1.0"
solana-hash = "4.6.0"
solana-instruction = "3.5.0"
solana-keypair = "3.1.2"
solana-message = "4.5.0"
solana-packet = "4.3.0"
solana-pubkey = { version = "4.3.0", default-features = false }
solana-sdk-ids = "3.1.0"
solana-short-vec = "3.3.0"
solana-signature = { version = "3.5.1", default-features = false }
solana-signer = "3.0.1"
solana-svm-transaction = "5.0.0"
solana-system-interface = { version = "3.3", features = ["alloc", "wincode"] }
solana-transaction = "4.2.0"
syn = "2.0.118"
tempfile = "3.27.0"
thiserror = "2.0.20"
wincode = { version = "0.6.1", features = ["derive"] }
wincode-dynamic = { version = "0.2.0" }

[profile.release]
split-debuginfo = "unpacked"
lto = "thin"

# Enable basic optimizations for unittests
[profile.test]
opt-level = 1

# Enable optimizations for procmacros for faster recompile
[profile.dev.build-override]
opt-level = 1
